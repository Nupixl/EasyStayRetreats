{
  "name": "easystay-retreats",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "preview": "npm run build && wrangler dev",
    "devlink:sync": "webflow devlink sync",
    "devlink:watch": "webflow devlink watch",
    "deploy": "webflow cloud deploy",
    "deploy:webflow": "npm run build && webflow cloud deploy",
    "workflow:sync": "./scripts/webflow-workflow.sh sync",
    "workflow:dev": "./scripts/webflow-workflow.sh dev",
    "workflow:build": "./scripts/webflow-workflow.sh build",
    "workflow:deploy": "./scripts/webflow-workflow.sh deploy",
    "workflow:watch": "./scripts/webflow-workflow.sh watch",
    "workflow:full": "./scripts/webflow-workflow.sh full",
    "webflow:build": "BASE_PATH=/app ASSETS_PREFIX=/app npm run build",
    "webflow:deploy": "npm run webflow:build && webflow cloud deploy"
  },
  "dependencies": {
    "next": "^15.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@opennextjs/cloudflare": "^1.8.2",
    "@webflow/webflow-cli": "^1.0.0",
    "@types/node": "^20.0.0",
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "eslint": "^9.0.0",
    "eslint-config-next": "^15.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.24.4"
  }
}
